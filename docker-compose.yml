version: '3.9'

services:
  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: example
    volumes:
      - /backup:/backup

  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - 8080:80